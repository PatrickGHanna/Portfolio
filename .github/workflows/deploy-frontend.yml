name: Deploy Frontend to Azure Static Web Apps

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-frontend.yml'
  workflow_dispatch:

env:
  NODE_VERSION: '20.x'
  APP_LOCATION: 'frontend'
  OUTPUT_LOCATION: 'dist'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy Frontend
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: ${{ env.APP_LOCATION }}/package-lock.json

      - name: Install dependencies
        working-directory: ${{ env.APP_LOCATION }}
        run: npm ci

      - name: Build frontend
        working-directory: ${{ env.APP_LOCATION }}
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL || vars.VITE_API_URL }}
        run: npm run build

      - name: Copy Static Web App Config
        run: |
          if [ -f "${{ env.APP_LOCATION }}/staticwebapp.config.json" ]; then
            cp "${{ env.APP_LOCATION }}/staticwebapp.config.json" "${{ env.APP_LOCATION }}/${{ env.OUTPUT_LOCATION }}/staticwebapp.config.json"
            echo "✓ Copied staticwebapp.config.json to dist folder"
          else
            echo "⚠ staticwebapp.config.json not found, skipping"
          fi

      - name: Verify dist folder contents before deployment
        run: |
          echo "=== Contents of dist folder ==="
          ls -la "${{ env.APP_LOCATION }}/${{ env.OUTPUT_LOCATION }}/"
          echo ""
          echo "=== index.html content ==="
          cat "${{ env.APP_LOCATION }}/${{ env.OUTPUT_LOCATION }}/index.html"
          echo ""
          echo "=== Checking for source index.html ==="
          if [ -f "${{ env.APP_LOCATION }}/index.html" ]; then
            echo "WARNING: Source index.html exists at ${{ env.APP_LOCATION }}/index.html"
            echo "Content:"
            cat "${{ env.APP_LOCATION }}/index.html"
          fi

      - name: Verify Build Output
        run: |
          echo "Checking build output in ${{ env.APP_LOCATION }}/${{ env.OUTPUT_LOCATION }}..."
          ls -la "${{ env.APP_LOCATION }}/${{ env.OUTPUT_LOCATION }}/" || echo "Directory not found"
          echo ""
          echo "Checking index.html content:"
          if [ -f "${{ env.APP_LOCATION }}/${{ env.OUTPUT_LOCATION }}/index.html" ]; then
            cat "${{ env.APP_LOCATION }}/${{ env.OUTPUT_LOCATION }}/index.html"
            echo ""
            echo "✓ Built index.html found"
            if grep -q "/src/main.jsx" "${{ env.APP_LOCATION }}/${{ env.OUTPUT_LOCATION }}/index.html"; then
              echo "::error::ERROR: Built index.html still references /src/main.jsx instead of built assets!"
              exit 1
            fi
          else
            echo "::error::ERROR: Built index.html not found!"
            exit 1
          fi

      - name: Validate Deployment Token
        id: validate_token
        run: |
          if [ -z "${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}" ]; then
            echo "::error::AZURE_STATIC_WEB_APPS_API_TOKEN secret is not set"
            echo ""
            echo "To get the deployment token:"
            echo "1. Go to Azure Portal: https://portal.azure.com"
            echo "2. Navigate to your Static Web App resource"
            echo "3. Click on 'Manage deployment token' in the left menu"
            echo "4. Copy the deployment token"
            echo "5. Go to GitHub: Settings → Secrets and variables → Actions"
            echo "6. Click 'New repository secret'"
            echo "7. Name: AZURE_STATIC_WEB_APPS_API_TOKEN"
            echo "8. Value: (paste the token)"
            echo "9. Click 'Add secret'"
            echo ""
            echo "Alternatively, you can get it via Azure CLI:"
            echo "  az staticwebapp secrets list --name <static-web-app-name> --resource-group <resource-group> --query 'properties.apiKey' -o tsv"
            exit 1
          fi
          echo "Deployment token found"

      - name: Prepare deployment package
        run: |
          # Create a clean deployment directory with only built files at root level
          # This ensures files are deployed to / not /dist
          mkdir -p deploy-root
          # Copy all contents from dist to deploy-root (not the dist folder itself)
          cp -r "${{ env.APP_LOCATION }}/${{ env.OUTPUT_LOCATION }}"/* deploy-root/ 2>/dev/null || true
          cp -r "${{ env.APP_LOCATION }}/${{ env.OUTPUT_LOCATION }}"/.[!.]* deploy-root/ 2>/dev/null || true
          echo "=== Deployment root contents ==="
          ls -la deploy-root/
          echo ""
          echo "=== index.html in deployment root ==="
          cat deploy-root/index.html
          echo ""
          echo "=== Verifying paths in index.html ==="
          if grep -q "/assets/" deploy-root/index.html; then
            echo "✓ index.html correctly references /assets/ paths"
          else
            echo "::error::index.html does not reference /assets/ paths correctly"
            exit 1
          fi
          if grep -q "/src/main.jsx" deploy-root/index.html; then
            echo "::error::ERROR: index.html still references /src/main.jsx!"
            exit 1
          fi

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: 'upload'
          app_location: ${{ env.APP_LOCATION }}
          output_location: ${{ env.OUTPUT_LOCATION }}
          app_artifact_location: deploy-root
          skip_app_build: true
